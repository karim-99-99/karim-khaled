# 📚 توثيق تنسيقات KaTeX للمعادلات الرياضية بالعربية

## 📍 الملف الأساسي
**المسار:** `src/index.css`

---

## 🎯 التنسيقات الرئيسية

### 1️⃣ دعم RTL (من اليمين لليسار)

```css
/* ========================================
   KaTeX RTL Support for Arabic
   ======================================== */

/* Force RTL rendering for all KaTeX elements */
.katex {
  direction: rtl !important;
  text-align: right !important;
}

/* RTL for react-katex components ($$...$$ format) */
.katex-display,
.katex-display > .katex {
  direction: rtl !important;
  text-align: right !important;
}

span > .katex {
  direction: rtl !important;
  text-align: right !important;
}
```

#### 🔍 الشرح:
- **`direction: rtl`**: يجعل الاتجاه من اليمين لليسار
- **تطبيق على جميع عناصر KaTeX**: يشمل المعادلات المضمنة والمستقلة
- **مثال النتيجة:**
  - `x²` يصبح `²x` (الأس على اليسار)
  - `√5` يصبح `٥√` (الجذر على اليمين)

---

### 2️⃣ تنسيقات الكسور (Fractions)

#### أ) الحاوية الرئيسية للكسر

```css
/* Ensure the fraction structure is correctly styled in RTL */
.katex .frac {
  display: inline-block !important;
  vertical-align: middle !important;
  text-align: center !important;
  position: relative !important;
  margin: 0 2px !important;
}
```

**الغرض:**
- `inline-block`: يجعل الكسر يظهر في السطر مع النص
- `vertical-align: middle`: يحاذي الكسر عمودياً في منتصف السطر
- `text-align: center`: يوسط محتوى الكسر
- `position: relative`: لتحديد موضع العناصر الداخلية

#### ب) العناصر الداخلية للكسر

```css
.katex .frac > span {
  display: block !important;
}
```

**الغرض:** يضمن أن العناصر الداخلية (البسط، الخط، المقام) تظهر كـ blocks

---

#### ج) البسط (Numerator) - الرقم الأعلى

```css
/* Fraction numerator (top number) - centered alignment */
.katex .frac-num {
  display: block !important;
  text-align: center !important;
  line-height: 1.2 !important;
  margin: 0 !important;
  padding: 0 !important;
  width: 100% !important;
}
```

**النتيجة:**
```
 ٣   ← البسط (في الأعلى، محاذاة مركزية)
───
 ٤
```

---

#### د) الخط الأفقي (Fraction Line)

```css
/* Fraction line (horizontal bar) - ensure visibility */
.katex .frac-line {
  border-bottom: 0.04em solid currentColor !important;
  border-top: none !important;
  display: block !important;
  width: 100% !important;
  height: 0 !important;
  margin: 0.1em 0 !important;
  padding: 0 !important;
  position: relative !important;
}
```

**نقاط مهمة:**
- `border-bottom: 0.04em solid currentColor`: يرسم الخط الأفقي
- `currentColor`: يستخدم لون النص الحالي
- `height: 0`: الخط ليس له ارتفاع فعلي
- `width: 100%`: يمتد عبر عرض الكسر بالكامل
- `margin: 0.1em 0`: مسافة صغيرة أعلى وأسفل الخط

**النتيجة:**
```
 ٣
───  ← الخط الأفقي (واضح وممتد)
 ٤
```

---

#### هـ) المقام (Denominator) - الرقم الأسفل

```css
/* Fraction denominator (bottom number) - centered alignment */
.katex .frac-den {
  display: block !important;
  text-align: center !important;
  line-height: 1.2 !important;
  margin: 0 !important;
  padding: 0 !important;
  width: 100% !important;
}
```

**النتيجة:**
```
 ٣
───
 ٤   ← المقام (في الأسفل، محاذاة مركزية)
```

---

#### و) التنسيقات الإضافية لمحاذاة النص

```css
/* Additional adjustments for text alignment and legibility in RTL */
.katex .frac-num,
.katex .frac-den {
  text-align: center;
}
```

**الغرض:** ضمان أن البسط والمقام دائماً في المنتصف

---

## 🔢 مثال كامل: تحويل وعرض كسر

### المدخلات (LaTeX):
```latex
\frac{3}{4}
```

### المعالجة:

1. **KaTeX يحول إلى HTML:**
```html
<span class="katex">
  <span class="frac">
    <span class="frac-num">3</span>
    <span class="frac-line"></span>
    <span class="frac-den">4</span>
  </span>
</span>
```

2. **تحويل الأرقام إلى عربية (في mathBlot.js):**
```
3 → ٣
4 → ٤
```

3. **تطبيق CSS (من index.css):**
- الكسر يظهر كـ `inline-block` في السطر
- البسط `٣` في الأعلى، محاذاة مركزية
- الخط الأفقي `───` واضح وممتد
- المقام `٤` في الأسفل، محاذاة مركزية

### النتيجة النهائية على الشاشة:
```
 ٣
───
 ٤
```

---

## 🎨 مشاكل تم حلها

### ❌ المشكلة 1: الأرقام المكدسة عمودياً
**قبل:**
```
٣
٤
```
(بدون خط أفقي، يبدو كأنه رقمان منفصلان)

**الحل:**
```css
.katex .frac-line {
  border-bottom: 0.04em solid !important;
  width: 100% !important;
}
```

**بعد:**
```
 ٣
───
 ٤
```

---

### ❌ المشكلة 2: الخط الأفقي غير مرئي

**السبب:** `height` مضبوط على قيمة غير صفرية أو `border` غير محدد

**الحل:**
```css
.katex .frac-line {
  border-bottom: 0.04em solid currentColor !important;
  height: 0 !important;
}
```

---

### ❌ المشكلة 3: البسط والمقام غير محاذيين

**الحل:**
```css
.katex .frac-num,
.katex .frac-den {
  display: block !important;
  text-align: center !important;
  width: 100% !important;
}
```

---

### ❌ المشكلة 4: المعادلات لا تدعم RTL

**الحل:**
```css
.katex {
  direction: rtl !important;
}
```

**النتيجة:**
- `x²` → `²x`
- `√5` → `٥√`
- الكسور تظهر بشكل صحيح في سياق RTL

---

## 📋 قائمة التحقق (Checklist) للكسور

عند إضافة كسر جديد، تأكد من:

- ✅ الخط الأفقي واضح ومرئي
- ✅ البسط فوق الخط
- ✅ المقام تحت الخط
- ✅ الأرقام محاذية في المنتصف
- ✅ الكسر محاذ عمودياً مع النص المحيط
- ✅ الأرقام عربية (٠-٩)
- ✅ الاتجاه RTL (للسياق العربي)

---

## 🔧 الملفات المرتبطة

### 1. `src/index.css`
**الدور:** تنسيقات CSS عمومية لجميع المعادلات

### 2. `src/components/mathBlot.js`
**الدور:** 
- تحويل LaTeX إلى HTML باستخدام KaTeX
- تحويل الأرقام من إنجليزية إلى عربية
- تطبيق تنسيقات CSS إضافية على DOM

### 3. `src/components/SimpleProfessionalMathEditor.jsx`
**الدور:**
- محرر Quill
- Modal للمعادلات
- تنسيقات CSS خاصة بالمحرر (في `<style>` tag)

### 4. `src/components/MathRenderer.jsx`
**الدور:**
- عرض المعادلات للطلاب
- إعادة تحويل LaTeX إلى HTML
- تطبيق تنسيقات CSS للعرض

---

## 🌍 دعم المتصفحات

هذه التنسيقات تعمل على:
- ✅ Chrome / Edge (Chromium)
- ✅ Firefox
- ✅ Safari
- ✅ جميع المتصفحات الحديثة

---

## 📝 ملاحظات مهمة

1. **استخدام `!important`:**
   - ضروري لتجاوز تنسيقات KaTeX الافتراضية
   - تأكد من عدم حذف `!important` من التنسيقات المهمة

2. **تحويل الأرقام:**
   - يتم في JavaScript (ليس CSS)
   - فقط بعد رندر KaTeX (ليس قبله)

3. **RTL Context:**
   - `direction: rtl` يؤثر على ترتيب العناصر
   - لا يؤثر على الكسور (تبقى عمودية)

4. **الوحدات:**
   - `em`: نسبية لحجم الخط (مفضلة)
   - `px`: ثابتة (تجنبها للكسور)

---

## 🚀 للمطورين الجدد

### كيفية إضافة تنسيق جديد:

1. **افتح `src/index.css`**
2. **أضف التنسيق في قسم KaTeX:**
```css
/* Your custom KaTeX styling */
.katex .your-class {
  /* properties */
}
```
3. **احفظ وأعد تحميل المتصفح (F5)**
4. **اختبر في:**
   - محرر الأسئلة (Admin)
   - صفحة الاختبار (Quiz)

---

## 📞 الدعم

إذا واجهت مشكلة في عرض الكسور:
1. تحقق من `src/index.css` (السطور 17-75)
2. تأكد من وجود `!important` في التنسيقات
3. افحص في Developer Tools:
   - Elements → ابحث عن `.katex .frac`
   - Computed → تحقق من التنسيقات المطبقة

---

تم التحديث: 2026-01-09
الإصدار: 2.0 (Enhanced RTL Support)
