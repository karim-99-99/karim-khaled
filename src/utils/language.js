// Language detection utility - Always return Arabic
export const isArabicBrowser = () => {
  return true; // Always Arabic - website is Arabic only
};

export const getDisplayText = (arabicText, englishText = '') => {
  return arabicText; // Always return Arabic text
};

// ee
